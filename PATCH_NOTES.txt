Главная — фиксы фильтра и карточек (2025‑09‑01)
===============================================
• app/page.tsx — заменён <form><select> на клиентский компонент CitySelect (URL ?city=... меняется при выборе)
• components/CitySelect.tsx — новый файл, 'use client'
• components/PriceTable.tsx — новая версия:
    - Заголовки: «Площадь» / «Цена м²»
    - Лейблы «от 50» и т.п. — жирные
    - Строки без цены скрываются
    - Поддержка диапазонов (min–max) и одиночных значений, форматирование через formatPrice
• lib/fields.ts
    - FieldOrder расширен (sort_order, display_name_ru?, visible?)
    - shortAddress теперь «Город, Адрес» (если оба есть)

Применение: распаковать в корень репозитория, закоммитить и запушить.
