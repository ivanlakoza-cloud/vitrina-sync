HOTFIX — lib/fields.ts (2025‑09‑01)
===================================
• Исправлен синтаксис функции export shortAddress(...) — предыдущая правка случайно повредила ключевые слова.
• shortAddress теперь корректно формирует заголовок «Город, Адрес» (или один из них).
• Тип FieldOrder оставлен расширенным: { sort_order: number; display_name_ru?: string; visible?: boolean }.

Применение: импортировать в корень репозитория и закоммитить.
