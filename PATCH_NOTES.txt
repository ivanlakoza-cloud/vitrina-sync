HOME hotfix — city in title + raw ranges (2025‑09‑01)
=====================================================
• lib/fields.ts
  - shortAddress(): теперь «Город, Адрес» (если оба есть), иначе один из них.
  - Сохранили расширенный тип FieldOrder { sort_order; display_name_ru?; visible? }.
• components/PriceTable.tsx
  - Показываем значения «как есть из БД» (например: 980 – 1180), без форматирования/парсинга.
  - Заголовки: «Площадь» / «Цена м²»; лейблы «от 50» жирные.
  - Пустые строки скрываются.

Применение: импортировать в корень репозитория, закоммитить и запушить.
