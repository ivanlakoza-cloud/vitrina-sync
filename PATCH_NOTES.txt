City column fix + title (2025‑09‑01)
====================================
• app/data.ts
  - fetchCities(): SELECT "City", NOT NULL по "City"; используется r.City
  - fetchList(): фильтр q.eq("City", city)
• app/page.tsx
  - Заголовок карточки включает город: `${City}, ${Адрес}` (с фолбэками)
• lib/fields.ts
  - shortAddress() теперь ищет город по ключам ["City", "city", "Город"]

Применение: импортировать в корень репозитория, закоммитить и запушить.
