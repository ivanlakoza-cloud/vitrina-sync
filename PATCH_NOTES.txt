Fix FieldOrder mapping (2025‑09‑01)
===================================
app/data.ts:
• В fetchFieldOrder() удалено свойство `column_name` из литерала объекта,
  теперь записываем только:
     { sort_order, display_name_ru, visible (по умолчанию true) }.
• Это соответствует типу FieldOrder и снимает ошибку TS в Vercel.
