# Bitrix24 widget build (v24)

- Single-file inline CSS+JS to minimize caching issues.
- Page path suggested: **/api/b24/soglasovanie-ceny** (recommended) or /b24/soglasovanie-ceny.
- Stick the URL into the widget settings as: `https://<your-domain>/api/b24/soglasovanie-ceny?v=24`

Diagnostics prints `boot v24` on the page header list.

